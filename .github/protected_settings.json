- name: Create JSON File
  run: |
    echo '{
      "commandToExecute": "export GITHUB_TOKEN=\"${{ secrets.TOKEN }}\" && sudo -u azureuser bash /home/azureuser/capstone_project/update_app.sh"
    }' > protected_settings.json
